{
  "name": "isaac-pi",
  "version": "1.0.0",
  "description": "Isaac's pi coding agent configuration — extensions, skills, and conventions.",
  "keywords": ["pi-package"],
  "scripts": {
    "postinstall": "cd .pi/extensions/pi-agent-scip && npm install --omit=dev"
  },
  "pi": {
    "extensions": [".pi/extensions"],
    "skills": [".pi/skills"]
  }
}
